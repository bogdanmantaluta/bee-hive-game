{
  "name": "bee-swarm-game",
  "version": "1.0.0",
  "description": "Bee Swarm Game Challenge",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && http-server -p 4200 -c-1 ./src",
    "test": "npm run build && jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "http-server": "^14.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0"
  }
}
